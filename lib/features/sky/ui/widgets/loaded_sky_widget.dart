import 'package:flutter/material.dart';

class LoadedSkyWidget extends StatelessWidget {
  const LoadedSkyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('success'));
  }
}
