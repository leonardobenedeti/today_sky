import 'package:flutter/material.dart';

class ErrorSkyWidget extends StatelessWidget {
  const ErrorSkyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('error'));
  }
}
